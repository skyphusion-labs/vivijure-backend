# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# envs
.venv/
venv/
.env
*.env

# model weights / caches (never commit)
*.safetensors
*.ckpt
*.pt
*.pth
*.bin
*.onnx
models/
hf-cache/
.cache/
# staged/bin-packed bake seed -- generated by the release / weights-base workflows,
# never committed (tens of GB). #537.
/deploy/seed-bins/
/deploy/train-bins/
/_seed/

# os / editor
.DS_Store
*.swp

# Real deploy.env holds YOUR RunPod + R2 keys (rendered from deploy.env.example); never commit it.
/deploy.env

# wrangler local dev state
.wrangler/
